# OPTIONAL — only needed if you want a server-side fallback key.
# Users can enter their own key directly in the app UI.
# If set, this key is used when no key is provided in the request.
GEMINI_API_KEY=
